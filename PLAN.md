# 实施计划

用户已确认：独立实验目录、完整产品构建与启动、真实 framework 修改与恢复。

## 交付内容

1. 个人 Git 主仓库与独立 AOSP 13 checkout，参考源码保持不变。
2. research_x86_64-userdebug 产品继承 sdk_phone_x86_64，包含 C++ research-info 工具。
3. frameworks/base 的 research/a13/prototype 分支增加只读 research-status 命令。
4. Python 标准库 lab 入口管理初始化、状态、构建、模拟器、验证和 Git 快照。
5. 实际完成基线启动、实验版启动、返回基线启动、重新启用实验版启动。
6. 原源码 Git 差异、原输出目录元数据与关键镜像哈希前后对比。
7. 文档、实测报告、可获取的 Git 历史与 prototype-v0.1 标签。

## 实施顺序

- 保存原环境状态；建立产品与工具代码。
- 测试路径隔离、脏状态拒绝、重复初始化和提交保存。
- 创建独立实验源码，固定所有实际项目提交，解析清单中的相对远端地址。
- 完整构建产品基线；启动并确认工具存在、核心命令不存在。
- 添加并保存核心功能提交；增量构建、启动并验证。
- 返回核心基线后重新构建、启动；确认工具保留而核心命令消失。
- 返回实验分支，重新构建、启动，保存最终快照。
- 校验参考环境，补齐文档与验收证据，打标签。

## 实现边界

只接入 AOSP 13，不下载 14/15，不修改参考源码，不调整 swap 或驱动。产品模块使用独立检出，不通过软链接共享正在编辑的文件。核心提交使用原生 Git 分支保存。后续新增 Android 版本只为已发现差异增加配置。

脚本不能用强制清理处理状态冲突。构建和启动失败必须继续诊断；模块编译成功不算完整原型通过。
